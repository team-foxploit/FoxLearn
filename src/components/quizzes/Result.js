// Exactly like Quizcard but edits disabled and correct answer and chosen ones are should be displayed
import React from 'react';

const Result = (props) => {
    return (
        <div />
    )
}

export default Result
